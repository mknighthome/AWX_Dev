---
- name: Configure Cisco IOS Switch
  hosts: cisco_routers
  gather_facts: false
  connections: network_cli

  tasks:
    - name: Set hostname via CLI
      cisco.ios.ios_config:
        lines:
          - hostname Core-Switch-01
        
    - name: Save running configuration
      cisco.ios.ios_config:
        save_when: modified
